updated_rpos = false
tilemap = layer_tilemap_get_id( "tiles" )

// todo set pos to spawn pos obj
// x = room_width / 2
// y = room_height / 1.5

if( item_count < 1 )
{
	// instance_create_layer( x,y,"instances",magic_wand_obj )
}